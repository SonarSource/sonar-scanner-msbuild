{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "github>SonarSource/renovate-config:quality-corelang-squad"
  ],
  "ignorePaths": [
    "its/projects/**"
  ],
  "nuget": {
    "description": "Keep global.json as the canonical SDK declaration; ignorePaths in the root does not override NuGet's built-in defaults",
    "ignorePaths": [
      "its/projects/**",
      "global.json"
    ]
  },
  "packageRules": [
    {
      "description": "Do not update Build dependency. It provides backward compatibility with MsBuild 14+",
      "matchPackageNames": [
        "Microsoft.Build**"
      ],
      "enabled": false
    },
    {
      "description": "Refresh NuGet lockfiles with .NET 10 after dependency updates",
      "matchManagers": [
        "nuget"
      ],
      "skipArtifactsUpdate": true,
      "constraints": {
        "dotnet": "10.0.100"
      },
      "postUpgradeTasks": {
        "executionMode": "update",
        "installTools": {
          "dotnet": {}
        },
        "commands": [
          "dotnet restore SonarScanner.MSBuild.sln --force-evaluate"
        ],
        "fileFilters": [
          "**/packages.lock.json"
        ]
      }
    }
  ]
}
